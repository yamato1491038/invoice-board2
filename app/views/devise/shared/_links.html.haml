- if controller_name != 'sessions'
  = link_to "ログイン", new_session_path(resource_name), class: 'Button--user'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "ユーザー登録", new_registration_path(resource_name), class: 'Button--user'
  %br/
