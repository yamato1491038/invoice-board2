.AccountPage
  .AccountPage__title
    登録しているユーザーでログイン
  .AccountPage__sign
    = render "devise/shared/links"
  .AccountPage__form
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .FormField
        .FormField__label--normal
          = f.label :email
        .FormField__input--normal
          = f.email_field :email, autofocus: true, class: 'Button--input'
        .FormField
          .FormField__label--normal
            = f.label :password
            %i (英数字8文字以上)
          .FormField__input--normal
            = f.password_field :password, autocomplete: "off",class: 'Button--input'
        .actions
          = f.submit "登録", class: 'Button--login'